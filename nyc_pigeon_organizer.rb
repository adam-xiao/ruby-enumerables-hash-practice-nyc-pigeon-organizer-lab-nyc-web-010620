def nyc_pigeon_organizer(data)
  first_reduction = data.reduce({}) do | memo, (key, value) |
    memo[key] = value

    second_reduction = ata.reduce({}) do | memo, (key, value) |
      memo[key] = value
    end
end
